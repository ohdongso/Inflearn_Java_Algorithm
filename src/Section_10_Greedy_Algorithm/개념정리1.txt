- 작성내용 x

-재귀함수:자기자신을 호출하는 함수
-스택프레임:재귀함수 사용시 생성되는 스택 행의 단위를 의미하고 (매개변수, 지역변수, 복귀주소) 등이 포함된다.
-이진수:10진수를 2로 나누어 질때까지 반복하여, 나머지를 마지막부터 순차적으로 적어주면 2진수를 구할수 있다.
-팩토리얼:5팩토리얼 == 5! == 5*4*3*2*1=120
-피보나치수열:1번쨰항 2번째항을 더해서 3번째항을 구하고 한 칸씩 옮겨서 다음항을 구해주는 알고리즘이다.
-메모리제이션:기록된 데이터 값을 활용해, 시간을 줄이는 개념을 뜻한다.
-이진트리:부모 > 왼쪽자식 노드 > 오른쪽 자식노드를 기본으로 가지는 노드다.
-DFS:깊이우선 탐색으로, root에서 왼쪽부터 끝까지 탐색한 뒤 막히면 뒤로 빽해서 오른쪽으로 이동한 뒤 계속 탐색을 이어간다.
	-전위순회:부모 > 왼쪽자식 > 오른쪽자식 순으로 탐색 ==> 부모가 기준이다.
	-중위순회:왼쪽자식 > 부모 > 오른쪽자식 순으로 탐색 ==> 왼쪽자식이 기준이다.
	-후위순회:왼쪽쪽자식 > 오른쪽자식 > 부모 순으로 탐색 ==> 왼쪽자식이 기준이다.
-BFS:넓이우선 탐색으로, 레벨탐색으로도 불리며, 루트에서 다음 레벨 전체를 탐색하고, 그다음 레벨 전체를 탐색하는 알고리즘이다.
-말단노드:자식이 없는노드
-최단거리구하기 문제는 BFS로 푼다. DFS는 자식 노드가 모두 존재할때만 가능해서 제약사항이 많다.
-인접행렬:그래프에서 어느 꼭짓점들이 변으로 연결되었는지 나타내는 정사각 행렬
-인접리스트:정점이 만개 이상일 경우 인접행렬로 구해주기에는 무리가 있어서 인접행렬 대안으로 사용 하는 알고리즘
각각의 정점 개수만큼 리스트 객체를 만든다. 그리고 각각의 정점이 움직일수 있는 정점의 경우의 수를 리스트에 담는다.
리스트에 저장된 값들은 갈 수 있는 경우만 저장하기 때문에 못가는 경우를 불필요하게 확인할 필요가 없다.
